{
  "name": "@rental-platform/worker",
  "version": "0.1.0",
  "private": true,
  "description": "Background jobs: visit-request expiry sweep. The rent-expiry reminder scheduler, payment reconciliation, and notification dispatch all ended up running in-process inside apps/api instead (see README.md) so they share its event bus — this process only has the one sweep left.",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/main.js",
    "start:dev": "ts-node-dev --respawn src/main.ts"
  },
  "dependencies": {
    "@prisma/adapter-pg": "^7.9.1",
    "@prisma/client": "^7.9.1",
    "node-cron": "^4.6.0",
    "bullmq": "^6.1.2",
    "ioredis": "^6.0.0"
  },
  "devDependencies": {
    "@types/node": "^24.13.3",
    "@types/node-cron": "^3.0.11",
    "ts-node-dev": "^2.0.0",
    "typescript": "^6.0.3"
  }
}
